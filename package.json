{
  "name": "price-notify-bot",
  "version": "0.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true,
  "scripts": {
    "start": "npx wrangler dev --env dev --test-scheduled",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250413.0",
    "@types/node": "20.12.7",
    "gts": "^5.3.1",
    "typescript": "^5.4.3",
    "wrangler": "~4.24.4"
  },
  "dependencies": {
    "decimal.js": "^10.4.3"
  }
}
